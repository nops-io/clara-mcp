name: Release MCP packages

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  package:
    name: Package Clara MCP plugins
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate package structure
        run: |
          test -f packages/claude/.claude-plugin/plugin.json
          test -f packages/cursor/.cursor-plugin/plugin.json
          test -d packages/cursor/.cursor/rules
          test -d shared/skills
          test -f shared/mcp/clara.mcp.json

      - name: Package MCP plugins
        run: |
          chmod +x scripts/package-all.sh
          chmod +x scripts/package-claude.sh
          chmod +x scripts/package-cursor.sh
          chmod +x scripts/current-version.sh || true

          ./scripts/package-all.sh

      - name: Create or update GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" dist/*.zip --clobber
          else
            gh release create "$TAG" dist/*.zip \
              --title "$TAG" \
              --notes "Clara MCP plugin packages for $TAG"
          fi
